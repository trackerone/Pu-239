name: Static Guard (unblock)

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
      - name: Run static guard (tempered rules)
        env:
          GUARD_MODE: fail
          GUARD_EXCLUDE: admin/adminer.php,tools/repo_sanity.php
          GUARD_FAIL_ON: merge_conflict_marker,short_open_tag,eval_usage,deprecated_mysql
        run: php tools/static_guard.php
